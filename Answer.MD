# 台科大資管 Python 考試作答區

---

## 基本資料

**學號：** B11xxxxxx  
**系級：** 資管系大一  
**姓名：** 王小明  

---

## 一、 原始碼分析報告 (Static Analysis) (共8小題，每小題3分，總共24分)

### 1.1 變數與邏輯對照表

| 原始變數名稱 | 代表意義 (Business Logic) | 建議修正名稱 |
|---|---|---|
| Q1 l1 (in load_proc) |一個暫存變數，用來儲存從檔案中讀取的所有行 | file_lines|
| Q2 d2 (in main) |一個全域變數，儲存所有圖書的資料清單，每本書的資料以字典形式存放，包含書名(t)、ISBN(i)、狀態(s) |library_data |
| Q3 c_res (in check) |一個函式，用來檢查某個 ISBN 是否已經存在於圖書清單中 |check_isbn |

### 1.2 壞味道 (Code Smells) 診斷

請列出原程式碼中至少 5 個嚴重的設計問題，並說明為什麼不好：

- **Q1：** 全域變數 d2 的使用，全域變數會導致程式的可讀性和可維護性降低，因為任何函式都可以隨意修改它，增加了除錯的難度，應將 d2 作為參數傳遞給需要的函式，或封裝在類別中作為屬性。
- **Q2：**未使用 with 語法處理檔案，如果程式在檔案操作期間發生錯誤，檔案可能無法正確關閉，導致資源洩漏，應使用 Python 的 with 語法來自動管理檔案的開啟與關閉。
- **Q3：**缺乏錯誤處理，如果檔案不存在或損壞或使用者輸入錯誤時，程式可能無法正確處理，在檔案操作和使用者輸入處加入 try-except 區塊，處理可能的例外情況。
- **Q4：**程式未模組化，程式碼的可讀性和可維護性差，難以進行單元測試，將程式拆分為多個模組或類別，例如將圖書管理邏輯封裝在一個 LibraryManager 類別中。
- **Q5：**資料儲存格式不安全，圖書資料以自定義的 @@ 和 ## 分隔符儲存，這種格式容易出錯且難以擴展，如果圖書名稱或其他欄位中包含分隔符，會導致資料解析錯誤，使用標準化的資料格式（如 JSON 或 CSV）來儲存和讀取資料。

---

## 二、 Vibe-Coding 協作紀錄 (共6小題，每小題3分，總共18分)

請記錄你如何引導 GitHub Copilot 進行重構。
重構的要求，請根據題目卷以及你發現的 Code Smells 來進行。

### 2.1 關鍵 Prompt 紀錄

請貼出你對 AI 下達過最有效的 3 個 Prompt：

```log
Prompt A： 

Prompt B： 

Prompt C：
```

### 2.2 AI 錯誤處理

1. AI 在撰寫過程中是否有出錯？（例如：漏寫了某個功能、語法錯誤）你是如何發現並請它修正的？

2. 發現的問題： (範例：AI 漏掉了 exit 時要印出分類統計的功能)

3. 修正的對話： (範例：我告訴它「請在結束前迴圈遍歷 JSON 資料並計算分類總額」)

---

## 三、 成果驗證與測試  (共2小題，每小題10分，總共20分)

確保你的 `modern_library.py` 功能正確。

### 3.1 測試結果驗證 (Terminal)

請貼上執行程式並輸入 show 後，顯示資料清單。(請從呼叫執行程式到結束程式完整的把操作對話複製到下框)

```log

```

### 3.2 產出的資料檔案格式

請貼上重構後生成的 `books.json` 前幾行內容：

```json
[
  {
    "title": "XXX",
    "isbn": "XXX",
    "status": "XXX"
  }
]
```

---

## 四、 Git 提交紀錄 (Commit History)  (共2小題，總共13分)

### 4.1. 請執行 `git log --oneline` 並將 Commit 紀錄貼在此處，以證明你的開發流程(此小題6分)


```log
# 請在此處貼上 git log 內容
```

### 4.2. GitHub Repository 連結(此小題7分)

將專案推送到 GitHub (Public) 若提交到 Private 連結不給分。 請設定 Repository 名稱為 `學號-0527-Final` Ex: B11526748-0527-Final **請用 - 不是 _**

```log
# 請貼上你的 Repo 網址
```

希望這兩份 Markdown 格式能幫你省下不少排版時間！祝你的考試順利舉行！

最後請保留整份專案資料夾歐！
